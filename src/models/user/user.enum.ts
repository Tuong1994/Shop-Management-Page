export enum EGender {
  MALE = 1,
  FEMALE = 2,
}

export enum ERole {
  OWNER = 1,
  MANAGER = 2,
  LEADER = 3,
  CASHIER = 4,
  STOCKER = 5,
  CUSTOMER_SERVICE = 6,
  JANITOR = 7,
  SECURITY = 8,
}

export enum EPermission {
  CREATE = "create",
  UPDATE = "update",
  REMOVE = "remove",
}
