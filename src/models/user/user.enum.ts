export enum EGender {
  MALE = 1,
  FEMALE = 2,
}

export enum ERole {
  MANAGER = 1,
  LEADER = 2,
  STAFF = 3,
}

export enum EPermission {
  CREATE = "create",
  UPDATE = "update",
  REMOVE = "remove",
}
