export enum EGender {
  MALE = 1,
  FEMALE = 2,
}

export enum ERole {
  MANAGER = 1,
  LEADER = 2,
  CASHIER = 3,
  STOCKER = 4,
  CUSTOMER_SERVICE = 5,
  JANITOR = 6,
  SECURITY = 7,
}

export enum EPermission {
  CREATE = "create",
  UPDATE = "update",
  REMOVE = "remove",
}
